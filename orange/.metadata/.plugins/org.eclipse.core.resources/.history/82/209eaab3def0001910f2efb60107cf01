package webCrawling;

import java.io.IOException;

import org.jsoup.Jsoup;
import org.jsoup.nodes.Document;
import org.jsoup.select.Elements;

public class weca {
	public static void main(String[] args) {
			String caurl = "http://www.weca.or.kr/donation/campaign.asp";
			String selector = "div.bo_list";
		    Document doc = null;
		    
		    try {
		        doc = Jsoup.connect(caurl).get(); // 1. get방식의 URL에 연결해서 가져온 값을 doc에 담는다.
		    } catch (IOException e) {
		        System.out.println(e.getMessage());
		    }
		    
		    //메인 요소 선택
		    Elements weca = doc.select("ul>li");
		    
		    //gfound title
		    String[] titles = weca.select("p.s_list_stit").html().split("\n");
		    for(int t=0; t<titles.length; t++) {
		    	System.out.println(titles[t]);
		    }
		    //gfound url
			String[] urls = weca.select("span.s_list_btn").html().split("\n");
			for(int g=0; g<urls.length; g++) {
				urls[g] = urls[g].substring(9, 92);
				System.out.println(urls[g]);
			}
			
			//gfound img
			String[] imgs = weca.select("div.s_list_phot>a").html().split("\n");
			for(int i=0; i<imgs.length; i++) {
				imgs[i] = imgs[i].substring(10, 135);
				System.out.println(imgs[i]);
			}
			
//			String gimg = gfound.select("div.s_list_phot>a").html();
//			System.out.println(gimg);
		    
	}

}
